export class Level3 {
    constructor(context, canvas) {
        this.context = context;
        this.canvas = canvas;
    }

    initialize() {
        // Le canvas est déjà blanc grâce au CSS
        // Pas besoin d'ajouter de fond ou d'éléments
    }
} 