class LevelManager {
    constructor() {
    }

    startLevel(levelId, scene) {
        switch(levelId) {
            case 1:
                this.setupLevel1(scene);
                break;
            case 2:
                this.setupLevel2(scene);
                break;
            case 3:
                this.setupLevel3(scene);
                break;
        }
    }

    setupLevel1(scene) {
        
    }

    setupLevel2(scene) {
        
    }

    setupLevel3(scene) {
        
    }
}
